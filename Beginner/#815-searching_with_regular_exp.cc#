#include <regex>
#include <iostream>
#include <string>
#include <fstream>
int main(){
    std::ifstream in{"file.txt"};
    if(!in) std::cerr<<"file.txt not found";
    std::regex pattern{R"(\w{2}\s*\d{5}(-\d{4})?)"}
    
}