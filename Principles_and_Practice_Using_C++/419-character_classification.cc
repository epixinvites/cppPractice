#include "std_lib_facilities.h"
using namespace std;
int main(){
    /*
     isspace(c) Is c a whitespace?
     isalpha(c) Is c a letter?
     isdigit(c) Is c a digit?
     isxdigit(c) Is c a hex digit?
     isupper(c)/islower(c) Is a uppercase/lowercase letter?
     isalnum(c) Is c a letter or digit?
     iscntrl(c) Is c a control character? (ASCII 0-31 and 127)
     ispunct(c) Is c not a letter, digit, whitespace, or invisible control character?
     isprint(c) Is c printable?
     isgraph(c) Is isalpha(c) or isdigit(c) or ispunct(c)

     toupper(c) c's uppercase equivalent
     tolower(c) c's lowercase equivalent
     */
}